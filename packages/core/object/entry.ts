import {
  entries,
} from './entries'

import {
  hasOwn,
} from './hasOwn'

import {
  keyIn,
} from './keyIn'

import {
  keys,
} from './keys'

import {
  map,
} from './map'

import {
  merge,
} from './merge'

import {
  omit,
} from './omit'

import {
  pick,
} from './pick'

export {
  entries,
  hasOwn,
  keyIn,
  keys,
  map,
  merge,
  omit,
  pick,
}

export default {
  entries,
  hasOwn,
  keyIn,
  keys,
  map,
  merge,
  omit,
  pick,
}
