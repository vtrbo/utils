import {
  deepMerge,
} from './deepMerge'

import {
  entries,
} from './entries'

import {
  hasOwn,
} from './hasOwn'

import {
  keyIn,
} from './keyIn'

import {
  keys,
} from './keys'

import {
  map,
} from './map'

import {
  pick,
} from './pick'

export {
  deepMerge,
  entries,
  hasOwn,
  keyIn,
  keys,
  map,
  pick,
}

export default {
  deepMerge,
  entries,
  hasOwn,
  keyIn,
  keys,
  map,
  pick,
}
